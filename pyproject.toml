[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "melo-news-backend"
version = "0.1.0"
requires-python = ">=3.9"

[project.scripts]
backend = "cli:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]
